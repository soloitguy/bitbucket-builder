**Build Agent for BitBucket Pipelines with Headless Chrome**

Used by BitBucket Pipelines.  When test scripts are run using Chrome, the agent spins up a headless version allowing karma etc to run the tests.
